diff --git a/config.def.h b/config.def.h
index a719e36..a49a7ef 100644
--- a/config.def.h
+++ b/config.def.h
@@ -85,24 +85,24 @@ static unsigned int tabspaces = 8;
 /* Terminal colors (16 first used in escape sequence) */
 static const char *colorname[] = {
 	/* 8 normal colors */
-	"black",
-	"red3",
-	"green3",
-	"yellow3",
-	"blue2",
-	"magenta3",
-	"cyan3",
-	"gray90",
+	"#1c1c1c",
+	"#ff005f",
+	"#a6e22e",
+	"#ffbe4a",
+	"#00bfff",
+	"#9e6ffe",
+	"#00a8b7",
+	"#d0d0d0",
 
 	/* 8 bright colors */
-	"gray50",
-	"red",
-	"green",
-	"yellow",
-	"#5c5cff",
-	"magenta",
-	"cyan",
-	"white",
+	"#808080",
+	"#d7005f",
+	"#7aac18",
+	"#d7a03e",
+	"#0f97ff",
+	"#8652ff",
+	"#63aab8",
+	"#bcbcbc",
 
 	[255] = 0,
 
diff --git a/st.c b/st.c
index fbcd9e0..907cf13 100644
--- a/st.c
+++ b/st.c
@@ -3855,7 +3855,7 @@ xdrawglyphfontspecs(const XftGlyphFontSpec *specs, Glyph base, int len, int x, i
 
 	/* Change basic system colors [0-7] to bright system colors [8-15] */
 	if ((base.mode & ATTR_BOLD_FAINT) == ATTR_BOLD && BETWEEN(base.fg, 0, 7))
-		fg = &dc.col[base.fg + 8];
+		fg = &dc.col[base.fg + 0];
 
 	if (IS_SET(MODE_REVERSE)) {
 		if (fg == &dc.col[defaultfg]) {
